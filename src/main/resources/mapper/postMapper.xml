<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.app.threetier.mapper.PostMapper">

    <insert id="insert" parameterType="PostVO">
        <selectKey keyProperty="id" resultType="Long" order="BEFORE">
            SELECT SEQ_POST.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO TBL_POST (ID, POST_TITLE, POST_CONTENT, MEMBER_ID)
        VALUES (#{id}, #{postTitle}, #{postContent}, #{memberId})
    </insert>

    <!-- 단일 게시글 조회-->
    <select id="select" parameterType="Long" resultType="PostResponseDTO">
        SELECT
        TBP.ID, TBP.POST_TITLE, TBP.POST_CONTENT, TBP.POST_READ_COUNT, TBP.MEMBER_ID,
        TBM.MEMBER_EMAIL, TBM.MEMBER_NAME
        FROM TBL_POST TBP
        JOIN TBL_MEMBER TBM
        ON TBP.MEMBER_ID = TBM.ID
        WHERE TBP.ID = #{id}
    </select>

    <!-- 전체 게시글 목록 조회: 모든 게시글 정보와 작성자 정보를 함께 조회합니다. -->
    <select id="selectAll" resultType="PostResponseDTO">
        SELECT
        TBP.ID, TBP.POST_TITLE, TBP.POST_CONTENT, TBP.POST_READ_COUNT, TBP.MEMBER_ID,
        TBM.MEMBER_EMAIL, TBM.MEMBER_NAME
        FROM TBL_POST TBP
        JOIN TBL_MEMBER TBM
        ON TBP.MEMBER_ID = TBM.ID
    </select>

    <!-- 특정 회원이 작성한 게시글 목록 조회 -->
    <select id="selectAllByMemberId" parameterType="Long" resultType="PostResponseDTO">
        SELECT
        TBP.ID, TBP.POST_TITLE, TBP.POST_CONTENT, TBP.POST_READ_COUNT, TBP.MEMBER_ID,
        TBM.MEMBER_EMAIL, TBM.MEMBER_NAME
        FROM TBL_POST TBP
        JOIN TBL_MEMBER TBM
        ON TBP.MEMBER_ID = TBM.ID
        WHERE TBP.MEMBER_ID = #{memberId}
    </select>

    <!-- 게시글 수정 -->
    <update id="update" parameterType="PostVO">
        UPDATE TBL_POST
        SET POST_TITLE = #{postTitle}, POST_CONTENT = #{postContent}
        WHERE ID = #{id}
    </update>

    <!-- 게시글 조회수 증가 -->
    <update id="updatePostReadCount" parameterType="Long">
        UPDATE TBL_POST
        SET POST_READ_COUNT = POST_READ_COUNT + 1
        WHERE ID = #{id}
    </update>

    <!-- 게시글 삭제 -->
    <delete id="delete" parameterType="Long">
        DELETE FROM TBL_POST
        WHERE ID = #{id}
    </delete>

</mapper>
